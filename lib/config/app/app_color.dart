import 'package:flutter/material.dart';

class AppColor {
  /// Color [AppColor.dark] return like dark color
  static const Color dark = Color(0xff1A1D26);

  /// Color [AppColor.primary] return like primary color
  static const Color primary = Color(0xffFE8160);

  /// Color [AppColor.yellow] return like yellow color
  static const Color yellow = Color(0xffFED060);

  /// Color [AppColor.red] return like red color
  static const Color red = Color(0xffE74C3C);

  /// Color [AppColor.darkLighter] return like dark but more lighter color
  static const Color darkLighter = Color(0xff1A1D26);

  /// Color [AppColor.white] return like more dense white color
  static const Color white = Colors.white;

  /// Color [AppColor.black] return like more dense black color
  static const Color black = Colors.black;

  /// Color [AppColor.whiteDarker] return like white but still darker color
  static const Color whiteDarker = Color(0xffE5E5E5);

  /// Color [AppColor.gray] return like white but 100% gray color
  static const Color gray = Color(0xffC0C0C0);

  /// Color [AppColor.grayWafer] return like gray but more wafer color
  static const Color grayWafer = Color(0xff9A9FAE);

  /// Color [AppColor.darkWafer] return like dark but more wafer color
  static const Color darkWafer = Color(0xff4A4E4D);
}
